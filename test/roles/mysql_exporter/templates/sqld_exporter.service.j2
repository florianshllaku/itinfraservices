[Unit]
Description=SQL Exporter
Wants=network.target
After=network.target

[Service]
User=mysql_exporter
Environment="DATA_SOURCE_NAME=wordpress:{{ prometheus_mysql_password }}@tcp(localhost:3306)/wp"
Type=simple
ExecStart=/opt/sqld_exporter/mysqld_exporter

[Install]
WantedBy=multi-user.target